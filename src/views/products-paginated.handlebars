<h1 class="m-2"> Products </h1>
<div class='d-flex flex-row justify-content-center' style='width: 100%;' >
  {{!-- {{log localUser}} --}}
  {{#each products}}
    {{> product-card this localUser=../localUser}}
  {{/each}}
</div>

<div style="width: 100%" class="d-flex flex-row justify-content-center">
  {{#if data.hasPrevPage}} <a href="?{{queryString}}page={{prevPage}}" class='btn btn-warning' style="margin: 1rem;"> Prev </a> {{/if}}
  {{#if data.hasNextPage}} <a href="?{{queryString}}page={{nextPage}}" class='btn btn-warning' style="margin: 1rem;"> Next </a> {{/if}}
</div>

<script src="/scripts/cart.js"></script>