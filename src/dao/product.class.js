class Product {
    constructor(title, photo, category, price, stock) {
      this.title = title;
      this.photo = photo;
      this.category = category;
      this.price = price;
      this.stock = stock;
    }
  }

export default Product